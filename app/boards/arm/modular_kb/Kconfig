config BOARD_ENABLE_DCDC
	bool "Enable DCDC mode"
	select SOC_DCDC_NRF52X
	default y
	depends on (BOARD_MODULAR_KB)

config BOARD_MODULAR_KB_CHARGER
	bool "Enable battery charger"
	default y
	depends on (BOARD_MODULAR_KB)
